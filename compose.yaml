services:
  url-shortener:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: url-shortener
    networks:
      - url-shortener-net
    ports:
      - "8089:80"
    restart: unless-stopped

networks:
  url-shortener-net:
    external: true
